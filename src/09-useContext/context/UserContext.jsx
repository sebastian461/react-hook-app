/*
 * Toda esta definición se puede traducir como un componente,
 * este componente encapsula a otro componentes como sus hijos y
 * a través del provider les comparte informaicón para que la puedan usar
 */
import { createContext } from "react";

export const UserContext = createContext();
